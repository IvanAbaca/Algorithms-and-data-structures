#include <stdio.h>
#include <stdlib.h>

#include "ListaH.h"

#define arch "archAlu.bin";

int main()
{/*

//    tLista lista;
//    char cad[5];
//
//    crear_lista(&lista);
//
//    poner_al_inicio(&lista, "Hola", 5);
//    poner_al_inicio(&lista, "que", 4);
//    poner_al_inicio(&lista, "tal?", 5);
//
//    while(!lista_vacia(&lista))
//    {
//        sacar_del_inicio(&lista, cad, sizeof(cad));
//        puts(cad);
//    }

    tLista lista;
    char cad[5];
    tAlumno alu[] = {{111, "asdasd", 2.3},
                     {222, "asd", 5.3},
                     {333, "asdsd", 7.3},
                     {444, "lkasmd", 9.1},
                    };


    crear_lista(&lista);


    poner_al_inicio(&lista, alu, sizeof(tAlumno));

    reduce_lista(&lista, &promedio, promedio_alu, &cant_alu);


//    poner_al_final(&lista, "Hola", 5);
//    poner_al_final(&lista, "que", 4);
//    poner_al_final(&lista, "tal?", 5);

    //ver_inicio_lista(&lista, cad, sizeof(cad));
    //ver_ultimo_lista(&lista, cad, sizeof(cad));
    //puts(cad);

//    while(!lista_vacia(&lista))
//    {
//        sacar_del_final(&lista, cad, sizeof(cad));
//        //sacar_un_elem(&lista, cad, sizeof(cad), "que", (void*)cmpCadenas);
//        puts(cad);
//    }

    //vaciar_lista(&lista);

    if(lista_vacia(&lista))
        puts("La lista esta vacia.");
    else
        puts("La lista tiene elementos.");
*/
    return 0;
}
