#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("Hello world!\n");

    r = primero(&lista);
    while(r)
    {
        //muestro
        r = siguiente(&lista);
    }

    return 0;
}
